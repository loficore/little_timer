// Wails runtime stub — used only in Vite dev mode (desktop).
// On Android / Wails builds, the real /wails/runtime.js is injected by the runtime.
export const Call = { ByID: () => { throw new Error("Wails runtime not available in Vite dev mode"); } };
export const CancellablePromise = class {};
export const Create = {};
